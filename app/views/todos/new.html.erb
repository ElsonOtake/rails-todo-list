<div class="form-container">
  <div class="form-card">
    <div class="form-header">
      <h1 class="form-title"><%= t("todos.new_todo") %></h1>
      <p class="form-subtitle"><%= t("todos.no_todos_text") %></p>
    </div>

    <%= render "form", todo: @todo %>
  </div>
</div>